%h1 Modify Invalid Members
%p The following members could not be added. Check the errors on each line, fix them, and add the members.
.invalid_members_form
  - form_tag update_members_path do
    %table
      %tr
        %th Name:
        %th Email:
        %th Company:
        %th Error:
      - @not_valid.each do |m|
        %tr
          %td= text_field_tag "member[][name]", m[0]
          %td= text_field_tag "member[][email]", m[1]
          %td= text_field_tag "member[][company]", m[2]
          %td= m[3]
    = submit_tag 'Add members'
    or
    = link_to 'Done', members_path