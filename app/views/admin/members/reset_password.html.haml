%h1 Reset Password!
%p 
  Are you sure you want to reset 
  = @member.name
  's password? This action will create a new password for the member which will be sent via the supplied email.
%table
  %tr
    %td= button_to "Yes", send_email_path(@member.id)
    %td
      or
      = link_to 'Cancel', members_path